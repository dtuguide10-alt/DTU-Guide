# Neon Postgres connection string (get it from https://neon.tech project dashboard)
DATABASE_URL=postgresql://user:password@ep-xxxx.aws.neon.tech/dbname?sslmode=require

# Admin dashboard password (set your own; required to access /admin)
ADMIN_PASSWORD=change-me
